export type MenuItemType = {
  id: number,
  name: string,
  url: string
}

export type PostType = {
  id: number,
  title: string,
  date: string,
  category: string,
  summary: string,
  url: string,
}

export type WorkType = {
  id: number,
  title: string,
  year: string,
  category: string,
  img: string,
  summary: string,
  url: string,
}
